<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>Agregar Superhéroe</title>
    <style>
      body {
        font-family: sans-serif;
        padding: 20px;
      }
      form {
        max-width: 600px;
        margin: auto;
        display: flex;
        flex-direction: column;
        gap: 12px;
      }
      label {
        font-weight: bold;
      }
      input,
      textarea {
        padding: 8px;
        font-size: 14px;
      }
      button {
        padding: 10px;
        background-color: #2b87ff;
        color: white;
        border: none;
        cursor: pointer;
      }
      button:hover {
        background-color: #155ab4;
      }
    </style>
  </head>
  <body>
    <h1>🦸‍♀️ Agregar un nuevo superhéroe</h1>

    <form action="/api/heroes/agregar" method="POST">
      <label for="nombreSuperHeroe">Nombre del Superhéroe *</label>
      <input
        type="text"
        name="nombreSuperHeroe"
        required
        minlength="3"
        maxlength="60"
        pattern="\S(.*\S)?"
      />

      <label for="nombreReal">Nombre Real *</label>
      <input
        type="text"
        name="nombreReal"
        required
        minlength="3"
        maxlength="60"
        pattern="\S(.*\S)?"
      />

      <label for="edad">Edad *</label>
      <input type="number" name="edad" required min="0" />

      <!-- Planeta de Origen -->
      <label for="planetaOrigen">Planeta de Origen</label>
      <input
        type="text"
        name="planetaOrigen"
        id="planetaOrigen"
        minlength="2"
        maxlength="60"
      />

      <!-- Debilidad -->
      <label for="debilidad">Debilidad</label>
      <input
        type="text"
        name="debilidad"
        id="debilidad"
        minlength="3"
        maxlength="60"
      />
      <label for="poderes">Poderes (separados por coma) *</label>
      <input
        type="text"
        name="poderes"
        id="poderes"
        required
        pattern="([^,]+,\s*){1,}[^,]+"
        title="Ingresá al menos dos poderes separados por coma."
      />

      <label for="aliados">Aliados (separados por coma)</label>
      <input type="text" name="aliados" minlength="2" maxlength="60" />

      <label for="enemigos">Enemigos (separados por coma)</label>
      <input type="text" name="enemigos" minlength="2" maxlength="60" />

      <label for="creador">Creador</label>
      <input type="text" name="creador" minlength="2" maxlength="60" />

      <button type="submit">Agregar Superhéroe</button>
    </form>
  </body>
</html>
