<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>Dashboard de Superhéroes</title>
    <style>
      body {
        font-family: sans-serif;
        padding: 20px;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
      }
      th,
      td {
        border: 1px solid #ccc;
        padding: 8px;
        text-align: left;
      }
      th {
        background-color: #f2f2f2;
      }
    </style>
  </head>
  <body>
    <h1>🦸‍♂️ Dashboard de Superhéroes</h1>

    <% if (superheroes.length === 0) { %>
    <p>No hay superhéroes registrados.</p>
    <% } else { %>
    <table>
      <thead>
        <tr>
          <th>Nombre</th>
          <th>Nombre Real</th>
          <th>Edad</th>
          <th>Planeta</th>
          <th>Poderes</th>
        </tr>
      </thead>
      <tbody>
        <% superheroes.forEach(h => { %>
        <tr>
          <td><%= h.Nombre %></td>
          <td><%= h['Nombre Real'] %></td>
          <td><%= h.Edad %></td>
          <td><%= h['planeta de Origen'] %></td>
          <td><%= h.Poderes.join(', ') %></td>
        </tr>
        <% }); %>
      </tbody>
    </table>
    <% } %>
  </body>
</html>
